# Node.js dependencies
# This directory contains all the packages installed by npm and can be very large.
# It should always be ignored and regenerated by running `npm install`.
/node_modules

# Build output
# This directory contains the bundled and optimized code for the extension.
# It is generated by running `npm run build` and should not be tracked.
/dist
extension.zip

# Log files
# These files are generated for debugging purposes and are specific to a user's machine.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
# These files can contain sensitive data like API keys.
.env
.env.local
.env.*.local

# IDE and editor directories
# These folders store user-specific settings for code editors like VS Code or WebStorm.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Operating System files
# System-specific files that should not be in a shared repository.
.DS_Store
Thumbs.db

# Optional Vite cache
# Vite may create a cache directory to speed up operations.
.vite/